from django.contrib import admin

from django.urls import (
    include,
    path,
)


urlpatterns = [

    # =====================================================
    # Django Administration
    # =====================================================

    path(
        "django-admin/",
        admin.site.urls,
    ),

    # =====================================================
    # Accounts / Authentication / User Management
    # =====================================================

    path(
        "",
        include(
            "accounts.urls"
        ),
    ),

    # =====================================================
    # Policy Management
    # =====================================================

    path(
        "",
        include(
            "policies.urls"
        ),
    ),

    # =====================================================
    # Training & Quiz
    # =====================================================

    path(
        "",
        include(
            "training.urls"
        ),
    ),

    # =====================================================
    # POS Security / Shift Handover
    # =====================================================

    path(
        "",
        include(
            "pos_security.urls"
        ),
    ),

    # =====================================================
    # Incident Management
    # =====================================================

    path(
        "",
        include(
            "incidents.urls"
        ),
    ),

    # =====================================================
    # Compliance
    # =====================================================

    path(
        "compliance/",
        include(
            "compliance.urls"
        ),
    ),

    # =====================================================
    # Training Needs
    # =====================================================

    path(
        "",
        include(
            "training_needs.urls"
        ),
    ),

    # =====================================================
    # Reports
    # =====================================================

    path(
        "",
        include(
            "reports.urls"
        ),
    ),

    # =====================================================
    # Notifications
    # =====================================================

    path(
        "",
        include(
            "notifications.urls"
        ),
    ),

    # =====================================================
    # Audit Logs
    # =====================================================

    path(
        "",
        include(
            "auditlog.urls"
        ),
    ),
]